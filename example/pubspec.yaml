name: streamdeck_example_plugin
publish_to: 'none'
description: A sample StreamDeck plugin created in Dart.
version: 0.1.0
repository: https://github.com/DanTup/streamdeck.dart

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  streamdeck:
    path: ../
  json_annotation: ^4.7.0

dev_dependencies:
  lints: ^2.0.1
